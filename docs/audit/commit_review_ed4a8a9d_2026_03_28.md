# 提交审查记录：`ed4a8a9d`

## 审查对象

- 提交：`ed4a8a9db0e291bc0eec69ffb09d80584c4961bd`
- 标题：`fix(driver): 修复 ASCII 端口边界解析并补齐分配失败防护`

## 结论

本次源码级复核确认仍有 `1` 个高风险遗留问题。

## 发现

- `高` 本提交修掉了 ASCII 分块解析的边界问题，但预登录延迟登录还是通过 `event_base_once(..., arg=user)` 直接持有裸 `interactive_t *`，当连接在回调执行前关闭时，登录回调仍可能落到已释放对象上。问题点位于 `src/comm.cc:new_conn_handler`。这个问题后续由 `95eab12e` 修复。

## 备注

- 本提交自身针对 ASCII 分块和分配失败路径的修复方向是对的，遗留风险集中在尚未一起处理的登录事件生命周期。
